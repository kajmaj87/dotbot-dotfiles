- defaults:
    link:
      relink: true

- clean: ['~']

- create:
  - ~/bin
  - ~/tools
  - ~/projects
  - ~/.config/vim
  - ~/.local/share/vim/backup

- link:
    ~/.aliases: aliases
    ~/bin/auto_update.sh: auto_update.sh
    ~/.environment: environment
    ~/.functions: functions
    ~/.gitconfig: gitconfig
    ~/.vim/vimrc:
        path: vim/vimrc
        create: true
    ~/.zshrc: zshrc
    ~/.p10k.zsh: p10k.zsh
    ~/.config/redshift:
        path: config/redshift/
        create: true
    ~/.config/vim/plugins: vim/plugins
- shell:
  - [git submodule update --init --recursive, Installing submodules]
  - [curl -L git.io/antigen > antigen.zsh]
  - 
    command: '(! [[ $SHELL =~ "zsh" ]] && chsh -s $(which zsh)) || true'
    stdin: true
    stdout: true
    stderr: true
    description: Change shell to zsh if not already
    quiet: false
  - [curl -fLo ~/.vim/autoload/plug.vim --create-dirs https://raw.githubusercontent.com/junegunn/vim-plug/master/plug.vim, Installing vim-plug]
  - ["vim +PlugInstall +qall", Installing plugins]
  - ["vim +PlugUpdate +PlugClean! +qa", Updating and cleaning old plugins]
